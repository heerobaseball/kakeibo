{
  "name": "kakeibo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --ip 0.0.0.0",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.50.0"
  }
}
